# Home Assistant Configuration — Google Home Redesign
# Add/modify these sections in your /config/configuration.yaml

frontend:
  themes: !include_dir_merge_named themes
  extra_module_url:
    - /local/gh-tokens.js
    - /local/gh-responsive.js
  # Optional: Set theme globally
  # theme: "google_home_light"
